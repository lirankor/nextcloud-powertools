"""Action handler registry (M2).

Placeholder package for milestone M2, which will define ``ACTIONS``
(action-name -> Handler), the ``Handler`` protocol / ``HandlerContext`` in
``base.py``, and the archive/compress/render handlers. Intentionally empty for
M1 beyond this docstring so the import path exists.
"""
